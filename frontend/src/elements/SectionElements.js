import styled from "styled-components"

//Main section element which we use througout the project
export const MainSection = styled.section`
  background-color: ${props => props.color};
  border-bottom: 1px solid #c1c1c1;
`
